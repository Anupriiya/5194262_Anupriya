#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    char s[105],f[105];
    scanf("%s" ,s);
    scanf("%s",f);
    int n = strlen(s);
    for(int i = 0; i<n;i++) {
        if(s[i] == f[i]) {
            printf("0");
        
        }
        else {
            printf("1");
        }
        
    }
    return 0;
}